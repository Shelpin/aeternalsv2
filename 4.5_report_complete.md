🛠️ Valhalla Victory Plan — Final Phased Launch Strategy (No Forks, No Maybes)
This is the final, hardened, expert-validated one-path, zero-fork execution plan to take the ElizaOS autonomous agent network from its current status to full message-capable, bot-to-bot communication. Each phase ends with clear success criteria and must be fully validated before proceeding.
🧩 Phase I: Structural Fixes and Codebase Hardening
Goals:
Fix token mismatches and agent ID inconsistencies
Patch message handling actions
Prepare proper environment for runtime correctness
Steps:
Update character JSONs
Add an explicit agentId field in each character JSON (match the env name). For example:
json
Copy
Edit
{
  "name": "ETHMemeLord9000",
  "agentId": "eth_memelord_9000",
  ...
}
Update runtime patching script
In patches/apply-patches.js, append a new message handler registration:
ts
Copy
Edit
runtime.registerAction({
  name: "handleMessage",
  description: "Message handler",
  handler: runtime.handleMessage.bind(runtime),
  validate: () => true,
  examples: []
});
(This ensures a dedicated action for inter-agent messages, preventing reliance on any fallback logic.)
Fix plugin logging bug
In packages/telegram-multiagent/src/TelegramMultiAgentPlugin.ts, replace the problematic code that logs available actions. Change:
ts
Copy
Edit
Object.keys(this.runtime.actions)
to:
ts
Copy
Edit
this.runtime.actions.map(a => a.name)
This will log action names instead of array indices.
Align token environment variables
In the .env file, ensure each bot’s Telegram token is defined with a key matching its agentId. For example:
env
Copy
Edit
TELEGRAM_BOT_TOKEN_eth_memelord_9000=<TOKEN_FOR_ETHMemeLord>
TELEGRAM_BOT_TOKEN_bag_flipper_9000=<TOKEN_FOR_BagFlipper>
... (and so on for all 6 agents) ...
Confirm that the Telegram plugin will read from environment variables named TELEGRAM_BOT_TOKEN_${agentId} for each agent. (Update the plugin code if necessary to use this naming convention.)
Fix relay agent ID hashing
In the relay server code (e.g. in relay-server/server.js where the message JSON is constructed), replace the numeric ID logic with a deterministic unique identifier. For example, replace:
js
Copy
Edit
id: parseInt(agent_id.replace(/\D/g, ''), 10)
with a hash-based approach:
js
Copy
Edit
id: require('crypto').createHash('md5').update(agent_id).digest('hex').slice(0, 8)
This uses the first 8 hex characters of an MD5 hash of the agent_id as a unique ID, avoiding collisions (e.g. both eth_memelord_9000 and bag_flipper_9000 will no longer collapse to 9000).
(After making code changes above, rebuild or restart the relevant components so these fixes take effect.)
✅ Validation:
The character JSON files and the .env entries are aligned (each agent JSON has the correct agentId, and .env has a corresponding token for that ID).
Running the patch script on an agent logs no errors – the custom handleMessage action is successfully registered.
The Telegram plugin’s log now shows readable action names (e.g. "Available runtime.actions: [\"GET_PRICE\",\"handleMessage\",...]" instead of numeric keys).
On agent startup, each bot finds its token without error (no “token missing” warnings), indicating the env variable naming is correct.
🚀 Phase II: Controlled Agent Launch
Goals:
Launch all six agents in a clean, port-specific manner (each agent on a fixed port to prevent conflicts)
eth_memelord_9000 on port 3000
bag_flipper_9000 on port 3001
linda_evangelist_88 on port 3002
vc_shark_99 on port 3003
bitcoin_maxi_420 on port 3004
code_samurai_77 on port 3005
Ensure each agent connects to the relay server with the correct token and Telegram plugin enabled.
Steps:
Stop all agents and clear ports
Make sure no old agent processes are running and free up the TCP ports 3000–3007 (covering our range plus a buffer):
bash
Copy
Edit
pkill -f patches/start-agent-with-patches || true
for port in {3000..3007}; do 
    fuser -k ${port}/tcp || true
done
Start the relay server first
Launch the relay server before any agents (it runs on port 4000 by default):
bash
Copy
Edit
cd /root/eliza/relay-server 
PORT=4000 node server.js &   # start relay in background
sleep 3                      # give it a moment to fully initialize
curl http://localhost:4000/health   # quick health check (should return status OK)
Ensure the relay server is running and accessible. The health check should return an OK status (and initially zero agents).
Launch each agent sequentially
For each agent, run its startup with the appropriate environment and port. Example for the first agent (eth_memelord_9000 on port 3000):
bash
Copy
Edit
export AGENT_ID=eth_memelord_9000 
export NODE_OPTIONS="--max-old-space-size=512 --expose-gc"
node patches/start-agent-with-patches.js --isRoot \
    --characters=packages/agent/src/characters/$AGENT_ID.json \
    --clients=@elizaos/client-telegram \
    --plugins=@elizaos/telegram-multiagent \
    --port=3000 --log-level=debug > logs/$AGENT_ID.log 2>&1 &
sleep 3
Repeat the above for each agent, substituting the correct AGENT_ID and --port. (Use the mapping in Goals: e.g., bag_flipper_9000 with port 3001, etc.) Wait a few seconds between launching each agent to avoid race conditions on ports or CPU. Each agent will run in the background and log to its own file in /root/eliza/logs/.
✅ Validation:
After all agents are started, running curl http://localhost:4000/health returns a JSON showing "agents":6 and an agents_list containing all six agent IDs.
Checking each agent’s log (e.g., logs/eth_memelord_9000.log) shows that:
✅ The TelegramMultiAgentPlugin initialized fully (no missing token errors).
✅ The agent connected to the relay (you should see a log line about a successful heartbeat or connection to localhost:4000).
✅ The bot is listening on the expected port (look for a log line confirming it bound to port 300X).
The relay server log (/root/eliza/relay-server/logs/... if applicable, or console output) shows each agent registering/heartbeating, indicating the relay knows about all agents.
📡 Phase III: Message Relay Debug & Processing Activation
Goals:
Ensure each agent is polling the relay and able to receive messages from it
Achieve a successful end-to-end message round-trip between two agents (via the relay)
Steps:
Verify polling configuration in the agent plugin
Open the Telegram relay client code (e.g., packages/telegram-multiagent/src/TelegramRelay.ts) and confirm that it periodically checks for new messages from the relay. There should be a polling interval set, for example:
ts
Copy
Edit
// Inside TelegramRelay initialization or start method
this.pollingInterval = setInterval(() => this.fetchUpdates(), 2000);
This ensures each agent asks the relay server for new messages every 2 seconds. If this code is missing or the interval is too infrequent, add or adjust it accordingly.
Enhance processUpdates() logging
In the same file or wherever incoming relay updates are processed, improve the logging to trace message flow. For instance, log a message when updates are fetched and for each message in the updates, log its sender and content. For example:
ts
Copy
Edit
this.logger.info(`[RELAY] Processing ${updates.length} updates`);
// ... within loop:
this.logger.info(`[RELAY] Received message from ${update.message.sender_agent_id}: ${update.message.text}`);
Ensure that if a message handler function is called (e.g. runtime.handleMessage), it's wrapped in a log or error handler so you can see if it executes. These logs will help confirm that messages retrieved from the relay are being handed off to the agent’s runtime.
Send a test message via the relay API
Use curl to simulate one bot sending a message to others through the relay. For example, have eth_memelord_9000 send a message:
bash
Copy
Edit
curl -X POST http://localhost:4000/sendMessage \
     -H "Authorization: Bearer elizaos-secure-relay-key" \
     -H "Content-Type: application/json" \
     -d '{"agent_id":"eth_memelord_9000", "chat_id":"dummy", "text":"LFG Valhalla"}'
This API call tells the relay that eth_memelord_9000 (acting as a sender) has a message "LFG Valhalla". The chat_id here can be any placeholder or context (since bots don’t have a shared chat, “dummy” is fine). The relay should accept this and queue the message for the other five agents.
Watch the agent logs for message processing
Pick one of the other agents (say, bag_flipper_9000) and observe its log in real-time to see if it receives the test message:
bash
Copy
Edit
tail -f logs/bag_flipper_9000.log
In the log output, look for evidence of the incoming message being processed. You should see log entries such as:
RELAY: Received 1 new messages (indicating the agent fetched an update from the relay)
RELAY: Received message from eth_memelord_9000 (the log we added, showing the sender)
runtime.handleMessage called or similar (showing the agent’s runtime is invoking the message handler to process the message)
You may also see the content of the message ("LFG Valhalla") in the logs if you logged it. Repeat this log check for multiple agents if needed to ensure all intended recipients picked up the message.
✅ Validation:
At least one agent’s log confirms it received a message from another agent via the relay (the log lines from step 4 appear as expected).
Internal message counters (if any are logged) show that at least one message was processed (e.g., a log line like Message stats: Processed = 1 or an increment in a messages received count).
(If Telegram front-end is enabled for testing) You observe one bot’s message cause a reaction or output from another in their Telegram chats. In other words, the bots are now effectively “seeing” each other’s messages through the relay.
🧠 Phase IV: Final Sanity Checks & Monitoring
Goals:
Establish full lifecycle observability for the system
Fix or improve the agent monitoring so we can trust the status reports
Steps:
Fix the monitor_agents.sh script (if one is being used to check agent status)
Update the script to detect running agent processes by name/port rather than relying on stale PID files. For example, instead of checking saved PID files, have it search the process list:
bash
Copy
Edit
# Example: list all processes for our agent launcher
ps -ef | grep 'start-agent-with-patches' | grep -v grep
Remove or update any logic that reads old PID files. Ensure that if an agent process dies, the monitor script can accurately reflect that by checking the live processes.
Add a watchdog mechanism
Implement a simple watchdog script or process to keep the agents running: for example, a script that periodically checks if each agent’s process is alive (using a similar ps or health-check approach) and if not, restarts the agent and logs an alert. This could be as simple as a cron job or a small Node script that pings the /health endpoint and restarts agents that disappear, as well as notifying (perhaps via a log or Telegram message) if an agent goes down or the relay disconnects. This ensures high availability of the bot network.
Document the deployment
Consolidate all configuration and launch instructions in the repository’s README.md or a deployment guide. Document the exact sequence to start the relay and agents, the environment variables required (like the Telegram tokens and any special flags such as FORCE_EXACT_PORT if used), and how to run the monitoring scripts. This will help future deployments or team members understand the setup and avoid regressions.
✅ Validation:
The modified monitor_agents.sh (or equivalent status check) correctly shows all agents as running (e.g., no false "STOPPED" reports when they are actually running). Running it should list 6 agent processes up.
The watchdog mechanism is tested by intentionally stopping one agent (or simulating a crash) and verifying that it gets restarted automatically and an alert/notification is produced. After implementation, all agents stay up for an extended period (e.g. >15 minutes) without manual intervention, indicating stability.
Logs are continuously generated for each agent (e.g., periodic heartbeat or polling info), confirming the system is active. No unexpected errors or crashes appear during the soak period.
🎯 Phase V: Agent-to-Agent Interaction End-to-End Test
Goals:
Demonstrate a full end-to-end dialogue between agents via the relay (final proof of concept)
Observe the distinct personalities responding, validating that each agent’s unique character is intact while engaging in conversation
Steps:
Create a Telegram group (sandbox) with all 6 bots
Using a Telegram client, create a new group chat and invite all six bot users (you’ll need to use their Telegram usernames, which correspond to the bot tokens). Ensure the group settings allow bots and that each bot has been started (via /start or having privacy mode off, if applicable). This group will serve as a visible arena for their interaction.
Send an introductory message from one agent
Choose one bot to initiate the conversation. Using the relay API, send a message from that bot into the group. For example, call the /sendMessage endpoint with agent_id of the initiator bot and chat_id set to the group chat ID (which you can get from Telegram by inviting a bot that echoes the chat ID or via the Telegram API). For instance, have eth_memelord_9000 send a greeting or provocative statement to the group. This simulates that bot speaking in the group.
Observe the other agents’ responses
The relay server should distribute the message to all the other agents. Each receiving agent’s logic/personality may cause them to respond (through whatever built-in triggers or logic they have when they see a message). Watch the Telegram group chat to see if the other bots reply to the initial message. Additionally, monitor each agent’s log to confirm they processed the incoming message (each should log the received message via handleMessage). They might perform actions or send their own messages via the relay in response.
(Optional) Use the kickstarter feature for prompts
If the bots are passive unless prompted, use any available “kickstarter” or predefined prompt feature of ElizaOS to stimulate conversation. This could mean configuring an initial message or behavior script for each agent so that once one speaks, others have some programmed reaction or reply. Enable or trigger that feature so that the interaction doesn’t stall after a single message. This ensures a lively back-and-forth exchange, showcasing multi-agent dialogue.
✅ Victory Criteria:
One agent’s message is successfully relayed and delivered to all other agents.
At least one of the other agents generates a reply (through the relay) back into the conversation, without human intervention. Ideally, multiple replies happen, reflecting different personalities.
The logs for each agent confirm the message handling: you see the original message being handled by each recipient (handleMessage executed for each), and subsequent messages being sent out by those agents in reply.
In the Telegram group, you can see the conversation unfold: the bots are effectively talking to each other through the relay, completing the end-to-end proof of the system.
🏁 Final Victory Condition
At this point, all bots are:
Online (processes running and stable)
Connected (each linked to the relay and communicating)
Patched (code fixes applied for actions, tokens, IDs, etc.)
Talking to each other (via the relay server, bypassing Telegram’s restrictions)
Handling messages correctly (each agent processes incoming messages as intended)
You’re now in Valhalla. LFG. ⚔️